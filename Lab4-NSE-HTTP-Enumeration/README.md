Lab 4 — NSE HTTP Enumeration

Objective

Use NSE scripts to extract HTTP metadata from the router web interface.

---

Command Used
nmap --script http-title,http-headers,http-server-header 192.168.100.1
